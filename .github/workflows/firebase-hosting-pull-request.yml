# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
"on": pull_request
jobs:
  build_and_preview:
    if: "${{ github.event.pull_request.head.repo.full_name == github.repository }}"
    runs-on: ubuntu-latest
    env:
      NUXT_ENV_API_KEY: ${{ secrets.NUXT_ENV_API_KEY }}
      NUXT_ENV_AUTH_DOMAIN: ${{ secrets.NUXT_ENV_AUTH_DOMAIN }}
      NUXT_ENV_PROJECT_ID: ${{ secrets.NUXT_ENV_PROJECT_ID }}
      NUXT_ENV_APP_ID: ${{ secrets.NUXT_ENV_APP_ID }}
      NUXT_ENV_MEASUREMENT_ID: ${{ secrets.NUXT_ENV_MEASUREMENT_ID }}
      NUXT_ENV_STORAGE_BUCKET: ${{ secrets.NUXT_ENV_STORAGE_BUCKET }}
      NUXT_ENV_MESSAGING_SENDER_ID: ${{ secrets.REACT_APP_FIREBASE_KEY }}
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js 14
        uses: actions/setup-node@v1
        with:
          node-version: 14
      - name: generate
        run: |
          cd ./webapp
          yarn && yarn generate
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_GIT_DECK_720F1 }}"
          projectId: git-deck-720f1
          entryPoint: "./webapp"
